<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yucong.zhang.moduleb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/previewb_ic_launcher"
        android:label="@string/previewb_app_name"
        android:roundIcon="@mipmap/previewb_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/previewb_AppTheme">
        <activity android:name=".BMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>